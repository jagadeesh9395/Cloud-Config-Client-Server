server:
  port: 8050

spring:
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
  application:
    name: ClientApp

management:
  endpoints:
    web:
      exposure:
        include: '*'

